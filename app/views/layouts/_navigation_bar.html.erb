<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href=<%= root_path %>>Country Roads Tech</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to 'Pages', pages_path, class: 'nav-link active' %>
        </li>
        <li class="nav-item">
          <%= link_to 'Calendars', calendars_path, class: 'nav-link active' %>
        </li>
        <li class="nav-item">
          <%= link_to 'Events', calendar_events_path, class: 'nav-link active' %>
        </li>
        <li class="nav-item">
          <%= link_to 'Links', links_path, class: 'nav-link active' %>
        </li>
        <% if policy(:dashboard).index? %>
          <li class="nav-item">
            <%= link_to 'Admin Dashboard', dashboard_index_path, class: 'nav-link active' %>
          </li>
        <% end %>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            My Account
          </a>
          <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
            <% if current_user %>
              <li><%= link_to 'Edit Profile', edit_user_registration_path, class: 'dropdown-item' %></li>
              <li><hr class="dropdown-divider"></li>
              <li><%= link_to 'Sign Out', destroy_user_session_path, method: :delete, data: { confirm: 'Are you sure you want to sign out?' }, class: 'dropdown-item' %>
            <% else %>
              <li><%= link_to 'Sign In', new_user_session_path, class: 'dropdown-item' %></li>
              <li><%= link_to 'Sign Up', new_user_registration_path, class: 'dropdown-item' %></li>
            <% end %>
          </ul>
        </li>
      </ul>
      <%= form_tag search_index_path, method: :get, class: 'd-flex' do %>
        <%= text_field_tag :search, params[:search], type: 'search', placeholder: 'Search', class: 'form-control me-2' %>
        <%= submit_tag 'Search', name: nil, class: 'btn btn-outline-primary' %>
      <% end %>
    </div>
  </div>
</nav>
